spring:
  mvc:
    problemdetails:
      enabled: false
  application:
    name: operacoes-bancarias
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:coder-bank-cliente
    driver-class-name: org.h2.Driver
    username: user
    password: user
server:
  port: 8082